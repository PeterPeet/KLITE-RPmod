{
  "name": "klite-rpmod",
  "version": "0.9.0",
  "private": true,
  "description": "KLITE RPmod — tabletop-RPG roleplay layer for Esobold Esolite (KoboldAI Lite fork)",
  "license": "GPL-3.0",
  "scripts": {
    "build": "node scripts/build-bundle.js",
    "build:index": "node scripts/build-integrated-index.js",
    "test": "npm run build && node --test --test-force-exit tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "jsdom": "^28.1.0"
  }
}
